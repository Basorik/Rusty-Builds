// Phase 4.6: PoB unique item text parser (stub — implementation follows in Phase 4.6)
//
// This module will consume `InvertedTranslations` (from `data::stat_translations`)
// and convert the raw text blocks in `src-tauri/data/pob/Uniques/*.json` into
// `Vec<Item>`.  For now the types are in place so the module compiles.

pub use crate::data::stat_translations::{InvertedTranslations, ResolvedLine};
