# PoB XML exports go here.
#
# Each .xml file is a build exported from Path of Building Community (File → Copy Build Code,
# then paste into a .xml, or use File → Export Build to save directly).
#
# Run `bun run tool:gen-fixtures` to process all XMLs in this directory
# and generate/update the corresponding tests/fixtures/<name>.json files.
